group 'com.mattbertolini'
version '1.0.0-SNAPSHOT'

subprojects {
    apply plugin: 'java'

    ext.javaVersion = 11

    dependencies {
        implementation platform('io.dropwizard:dropwizard-bom:2.0.13')
        implementation platform('org.springframework:spring-framework-bom:5.2.7.RELEASE')
    }
}

wrapper {
    distributionType = Wrapper.DistributionType.ALL
}
